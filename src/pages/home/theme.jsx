import { createMuiTheme } from '@material-ui/core/styles'


const theme = createMuiTheme({
	palette: {
		primary: {
			main: '#82b1ff',
			contrastText: '#000',
		},
		secondary: {
			main: '#ff4081',
			contrastText: '#000',
		},
	},
})

export default theme
